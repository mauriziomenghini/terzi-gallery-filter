name: terzi_gallery_filter
description: Seleziona foto con regola dei terzi - 100% on-device
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  photo_manager: ^3.0.0
  google_mlkit_face_detection: ^0.9.0
  image: ^4.1.7

flutter:
  uses-material-design: true
